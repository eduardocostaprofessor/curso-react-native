import { StyleSheet } from "react-native"

export default StyleSheet.create({
    containerContent: {
        flex: 3,
        alignItems: 'center',
        backgroundColor: '#fff',
        paddingTop: 40,
        borderTopLeftRadius: 100,
    }
})